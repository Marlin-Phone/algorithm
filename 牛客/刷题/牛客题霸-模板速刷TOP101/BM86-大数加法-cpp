#include <bits/stdc++.h>
using namespace std;
/**
 * @nc app=nowcoder id=11ae12e8c6fe48f883cad618c2e81475 topic=295 question=1061819 lang=C++
 * 2025-10-04 20:23:54
 * https://www.nowcoder.com/practice/11ae12e8c6fe48f883cad618c2e81475?tpId=295&tqId=1061819
 * [BM86] 大数加法
 */

/** @nc code=start */

class Solution {
public:
    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     *
     * 计算两个数之和
     * @param s string字符串 表示第一个整数
     * @param t string字符串 表示第二个整数
     * @return string字符串
     */
    string solve(string s, string t) {
        int len1 = s.length();
        int len2 = t.length();
        if (len1 < len2) {
            swap(s, t);
            swap(len1, len2);
        }
        reverse(s.begin(), s.end());
        reverse(t.begin(), t.end());
        int carry = 0;
        string res = "";
        for(int i = 0; i < len2; i++){
            int sum = s[i] - '0' + t[i] - '0' + carry;
            res += sum % 10 + '0';
            carry = sum / 10;
        }
        for(int i = len2; i < len1; i++){
            int sum = s[i] - '0' + carry;
            res += sum % 10 + '0';
            carry = sum / 10;
        }
        if(carry) res += carry + '0';
        reverse(res.begin(), res.end());
        return res;
    }
};

/** @nc code=end */
