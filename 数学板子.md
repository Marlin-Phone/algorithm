# 数学

## 素数筛法

### 埃式筛 $O(nloglogn)$
```cpp
// 埃拉托斯特尼筛法 - 适用于较小范围 [0, n]
// 返回 [l, r] 区间内素数的个数
int eratosthenesSieve(int l, int r) {
    if (r < 2 || l > r) return 0;
    l = max(l, 2);
    vector<bool> isPrime(r + 1, true);
    isPrime[0] = isPrime[1] = false;
    for (int i = 2; i * i <= r; i++) {
        if (isPrime[i]) {
            for (int j = i * i; j <= r; j += i) {
                isPrime[j] = false;
            }
        }
    }
    int count = 0;
    for (int i = l; i <= r; i++) {
        if (isPrime[i]) count++;
    }
    return count;
}
```

### 区间筛 $O((r-l+1)loglogr + \sqrt{r}loglog\sqrt{r})$

```cpp
// 区间筛法 - 适用于大范围 [l, r]
// 返回 [l, r] 区间内素数的个数
int segmentedSieve(long long l, long long r) {
    if (r < 2 || l > r) return 0;
    l = max(l, 2LL);
    // 对于小范围，直接使用埃氏筛更高效
    if (r <= 10000000) {
        return eratosthenesSieve(static_cast<int>(l), static_cast<int>(r));
    }
    
    int sqrtR = static_cast<int>(sqrt(r)) + 1;
    // 埃式筛生成 sqrt(r) 以内的所有素数
    vector<bool> baseSieve(sqrtR + 1, true);
    baseSieve[0] = baseSieve[1] = false;
    vector<int> basePrimes;
    for (int i = 2; i <= sqrtR; i++) {
        if (baseSieve[i]) {
            basePrimes.push_back(i);
            for (int j = i * i; j <= sqrtR; j += i) {
                baseSieve[j] = false;
            }
        }
    }
    // 对区间 [l, r] 进行筛法
    vector<bool> segSieve(r - l + 1, true);
    for (int prime : basePrimes) {
        // 找到第一个 >= l 且能被 prime 整除的数
        int start = max(prime * prime, (l + prime - 1) / prime * prime);
        for (int j = start; j <= r; j += prime) {
            segSieve[j - l] = false;
        }
    }
    // 统计素数个数
    int count = 0;
    for (size_t i = 0; i < segSieve.size(); i++) {
        if (segSieve[i]) count++;
    }
    return count;
}
```

### 获得一个合数的所有质数因子$O(\sqrt{n})$

```cpp
// 返回 n 的所有质因子及其指数，按升序排列
vector<pair<long long,int>> factorize(long long n) {
    // 每个pair是(质因子, 指数)
    vector<pair<long long,int>> res;
    auto push = [&](long long p) {
        int cnt = 0;
        while (n % p == 0) { n /= p; ++cnt; }
        if (cnt) res.emplace_back(p, cnt);
    };
    // 2 单独处理，方便后面步长 2
    push(2);
    // 试除奇数到 sqrt(n)
    for (long long p = 3; p * p <= n; p += 2) push(p);
    // 如果还剩一个 >1 的质数
    if (n > 1) res.emplace_back(n, 1);
    return res;
}
```

### 最小质因子筛 预处理$O(n)$ 每次查询$O(logn)$

```cpp
const int N = 1e6;
int minp[N+1];          // minp[i] 表示 i 的最小质因子
vector<int> primes;
void sieve() {
    for (int i = 2; i <= N; ++i) {
        if (!minp[i]) {          // i 是质数
            minp[i] = i;
            primes.push_back(i);
        }
        for (int p : primes) {
            if (p > minp[i] || i * p > N) break;
            minp[i * p] = p;
        }
    }
}
// 快速分解 x≤N
vector<pair<int,int>> fastFactor(int x) {
    vector<pair<int,int>> res;
    while (x > 1) {
        int p = minp[x], cnt = 0;
        while (x % p == 0) { x /= p; ++cnt; }
        res.emplace_back(p, cnt);
    }
    return res;
}
```

### 倍数贡献法（用于因数/整除统计） 调和级数 $O(nln(n))$

```cpp
const int MAX = 1e6 + 10;  // 根据题目 A_i 上限设定
int cnt[MAX];      // cnt[x] = x 在数组中出现的次数
int total[MAX];    // total[x] = 数组中有多少个数能整除 x

// 倍数贡献（核心！）
for (int d = 1; d < MAX; d++) {
    if (cnt[d] == 0) continue;               // 优化：跳过未出现的数
    for (int k = d; k < MAX; k += d) {       // 枚举 d 的所有倍数
        total[k] += cnt[d];                  // d 能整除 k，贡献 cnt[d] 次
    }
}
```

